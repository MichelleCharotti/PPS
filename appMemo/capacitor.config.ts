import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'juego.de.memoria',
  appName: 'appMemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
